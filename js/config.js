export const CONFIG = {
  apiUrl: 'https://api.noroff.dev/api/v1/holidaze/',
  // Optional base path when the app is served from a subfolder (e.g. '/workflow-repo-ca')
  // Set to empty string for root. Update this to your project folder if needed.
  basePath: '/workflow-repo-ca',
};
